import type { Config } from "tailwindcss";

// Tailwind v4 reads theme tokens from CSS (@theme in app/globals.css).
// This file is kept only so shadcn's CLI has a config path to reference.
const config: Config = {};

export default config;
